export const HEAP = [
  {
    id: "kth-largest-array",
    title: "Kth Largest Element in an Array",
    difficulty: "Medium",
    topic: "Heap / Priority Queue",
    statement: `Given an integer array nums and an integer k, return the kth largest element in the array.\n\nNote that it is the kth largest element in sorted order, not the kth distinct element.\n\nExample:\nInput: nums = [3, 2, 1, 5, 6, 4], k = 2\nOutput: 5\nExplanation: Sorted descending is [6, 5, 4, 3, 2, 1]; the 2nd largest is 5.`,
    funcName: "find_kth_largest",
    starter: `def find_kth_largest(nums, k):\n    # Your code here\n    pass`,
    solution: `import heapq\n\ndef find_kth_largest(nums, k):\n    heap = []\n    for x in nums:\n        heapq.heappush(heap, x)\n        if len(heap) > k:\n            heapq.heappop(heap)\n    return heap[0]`,
    tests: [
      { call: "find_kth_largest([3, 2, 1, 5, 6, 4], 2)", expected: "5" },
      { call: "find_kth_largest([3, 2, 3, 1, 2, 4, 5, 5, 6], 4)", expected: "4" },
      { call: "find_kth_largest([1], 1)", expected: "1" },
      { call: "find_kth_largest([7, 7, 7], 2)", expected: "7" },
      { call: "find_kth_largest([2, 1], 2)", expected: "1" },
    ],
    hint: "Keep a min-heap of size k; the root is the kth largest.",
  },
  {
    id: "top-k-frequent",
    title: "Top K Frequent Elements",
    difficulty: "Medium",
    topic: "Heap / Priority Queue",
    statement: `Given an integer array nums and an integer k, return the k most frequent elements. Return the answer sorted in ascending order.\n\nExample:\nInput: nums = [1, 1, 1, 2, 2, 3], k = 2\nOutput: [1, 2]\nExplanation: 1 appears 3 times and 2 appears 2 times — those are the 2 most frequent. Sorted ascending gives [1, 2].`,
    funcName: "top_k_frequent",
    starter: `def top_k_frequent(nums, k):\n    # Your code here\n    pass`,
    solution: `import heapq\nfrom collections import Counter\n\ndef top_k_frequent(nums, k):\n    counts = Counter(nums)\n    top = heapq.nlargest(k, counts.keys(), key=lambda x: counts[x])\n    return sorted(top)`,
    tests: [
      { call: "top_k_frequent([1, 1, 1, 2, 2, 3], 2)", expected: "[1, 2]" },
      { call: "top_k_frequent([1], 1)", expected: "[1]" },
      { call: "top_k_frequent([4, 4, 4, 5, 5, 6], 2)", expected: "[4, 5]" },
      { call: "top_k_frequent([3, 0, 1, 0], 1)", expected: "[0]" },
      { call: "top_k_frequent([5, 5, 6, 6, 7], 3)", expected: "[5, 6, 7]" },
    ],
    hint: "Count with Counter, take the k keys with largest counts, then sort.",
  },
  {
    id: "k-closest-points",
    title: "K Closest Points to Origin",
    difficulty: "Medium",
    topic: "Heap / Priority Queue",
    statement: `Given an array of points where points[i] = [xi, yi] and an integer k, return the k closest points to the origin (0, 0), measured by Euclidean distance. Return the result sorted in ascending order (by x, then y).\n\nExample:\nInput: points = [[1, 3], [-2, 2]], k = 1\nOutput: [[-2, 2]]\nExplanation: Distance of (1,3) is sqrt(10); distance of (-2,2) is sqrt(8). The closer point is (-2,2).`,
    funcName: "k_closest",
    starter: `def k_closest(points, k):\n    # Your code here\n    pass`,
    solution: `import heapq\n\ndef k_closest(points, k):\n    heap = []\n    for x, y in points:\n        dist = x * x + y * y\n        heapq.heappush(heap, (-dist, x, y))\n        if len(heap) > k:\n            heapq.heappop(heap)\n    result = [[x, y] for (_, x, y) in heap]\n    return sorted(result)`,
    tests: [
      { call: "k_closest([[1, 3], [-2, 2]], 1)", expected: "[[-2, 2]]" },
      { call: "k_closest([[3, 3], [5, -1], [-2, 4]], 2)", expected: "[[-2, 4], [3, 3]]" },
      { call: "k_closest([[0, 1], [1, 0]], 2)", expected: "[[0, 1], [1, 0]]" },
      { call: "k_closest([[1, 1]], 1)", expected: "[[1, 1]]" },
      { call: "k_closest([[2, 2], [2, -2], [-2, 2]], 3)", expected: "[[-2, 2], [2, -2], [2, 2]]" },
    ],
    hint: "Max-heap of size k keyed by negated squared distance, then sort the survivors.",
  },
  {
    id: "merge-k-sorted",
    title: "Merge K Sorted Lists",
    difficulty: "Hard",
    topic: "Heap / Priority Queue",
    statement: `You are given an array of k sorted (ascending) integer lists. Merge them into one sorted list and return it.\n\nExample:\nInput: lists = [[1, 4, 5], [1, 3, 4], [2, 6]]\nOutput: [1, 1, 2, 3, 4, 4, 5, 6]`,
    funcName: "merge_k_lists",
    starter: `def merge_k_lists(lists):\n    # Your code here\n    pass`,
    solution: `import heapq\n\ndef merge_k_lists(lists):\n    heap = []\n    for i, lst in enumerate(lists):\n        if lst:\n            heapq.heappush(heap, (lst[0], i, 0))\n    result = []\n    while heap:\n        val, i, j = heapq.heappop(heap)\n        result.append(val)\n        if j + 1 < len(lists[i]):\n            heapq.heappush(heap, (lists[i][j + 1], i, j + 1))\n    return result`,
    tests: [
      { call: "merge_k_lists([[1, 4, 5], [1, 3, 4], [2, 6]])", expected: "[1, 1, 2, 3, 4, 4, 5, 6]" },
      { call: "merge_k_lists([])", expected: "[]" },
      { call: "merge_k_lists([[]])", expected: "[]" },
      { call: "merge_k_lists([[1, 2, 3]])", expected: "[1, 2, 3]" },
      { call: "merge_k_lists([[], [1], []])", expected: "[1]" },
      { call: "merge_k_lists([[5, 10], [1, 6, 7], [2]])", expected: "[1, 2, 5, 6, 7, 10]" },
    ],
    hint: "Push the head of each list into a heap keyed by (value, list-index, element-index).",
  },
  {
    id: "task-scheduler",
    title: "Task Scheduler",
    difficulty: "Medium",
    topic: "Heap / Priority Queue",
    statement: `Given a list of CPU tasks (single uppercase letters) and a cooldown n, each task takes one interval. Identical tasks must be separated by at least n intervals; idle intervals may be inserted. Return the minimum number of intervals required to finish all tasks.\n\nExample:\nInput: tasks = ["A", "A", "A", "B", "B", "B"], n = 2\nOutput: 8\nExplanation: A -> B -> idle -> A -> B -> idle -> A -> B (8 intervals).`,
    funcName: "least_interval",
    starter: `def least_interval(tasks, n):\n    # Your code here\n    pass`,
    solution: `from collections import Counter\n\ndef least_interval(tasks, n):\n    counts = Counter(tasks)\n    max_count = max(counts.values())\n    num_max = sum(1 for c in counts.values() if c == max_count)\n    candidate = (max_count - 1) * (n + 1) + num_max\n    return max(candidate, len(tasks))`,
    tests: [
      { call: 'least_interval(["A", "A", "A", "B", "B", "B"], 2)', expected: "8" },
      { call: 'least_interval(["A", "A", "A", "B", "B", "B"], 0)', expected: "6" },
      { call: 'least_interval(["A", "A", "A", "A", "A", "A", "B", "C", "D", "E", "F", "G"], 2)', expected: "16" },
      { call: 'least_interval(["A"], 5)', expected: "1" },
      { call: 'least_interval(["A", "B", "C", "D"], 2)', expected: "4" },
    ],
    hint: "The answer is determined by the most frequent task; fill gaps of size n, then take max with len(tasks).",
  },
  {
    id: "last-stone-weight",
    title: "Last Stone Weight",
    difficulty: "Easy",
    topic: "Heap / Priority Queue",
    statement: `You have stones with integer weights. Each turn, smash the two heaviest stones x <= y together: if x == y both are destroyed, otherwise the heavier becomes y - x. Return the weight of the last remaining stone, or 0 if none remain.\n\nExample:\nInput: stones = [2, 7, 4, 1, 8, 1]\nOutput: 1\nExplanation: 8&7 -> 1; now [2,4,1,1,1]; 4&2 -> 2; now [2,1,1,1]; 2&1 -> 1; now [1,1,1]; 1&1 -> 0; now [1]. Last stone is 1.`,
    funcName: "last_stone_weight",
    starter: `def last_stone_weight(stones):\n    # Your code here\n    pass`,
    solution: `import heapq\n\ndef last_stone_weight(stones):\n    heap = [-s for s in stones]\n    heapq.heapify(heap)\n    while len(heap) > 1:\n        y = -heapq.heappop(heap)\n        x = -heapq.heappop(heap)\n        if y != x:\n            heapq.heappush(heap, -(y - x))\n    return -heap[0] if heap else 0`,
    tests: [
      { call: "last_stone_weight([2, 7, 4, 1, 8, 1])", expected: "1" },
      { call: "last_stone_weight([1])", expected: "1" },
      { call: "last_stone_weight([2, 2])", expected: "0" },
      { call: "last_stone_weight([3, 7, 2])", expected: "2" },
      { call: "last_stone_weight([10, 4, 4])", expected: "2" },
    ],
    hint: "Use a max-heap (negate values); repeatedly smash the two largest.",
  },
  {
    id: "kth-largest-in-stream",
    title: "Kth Largest Element in a Stream",
    difficulty: "Easy",
    topic: "Heap / Priority Queue",
    statement: `Design a structure that tracks the kth largest element in a stream. Given k, an initial list of numbers, and a list of values to add one at a time, return a list containing the kth largest element after each add.\n\nExample:\nInput: k = 3, initial = [4, 5, 8, 2], adds = [3, 5, 10, 9, 4]\nOutput: [4, 5, 5, 8, 8]\nExplanation: After adding 3 the 3rd largest is 4; after 5 it is 5; after 10 it is 5; after 9 it is 8; after 4 it is 8.`,
    funcName: "kth_largest_stream",
    starter: `def kth_largest_stream(k, initial, adds):\n    # Your code here\n    pass`,
    solution: `import heapq\n\ndef kth_largest_stream(k, initial, adds):\n    heap = list(initial)\n    heapq.heapify(heap)\n    while len(heap) > k:\n        heapq.heappop(heap)\n    result = []\n    for val in adds:\n        heapq.heappush(heap, val)\n        if len(heap) > k:\n            heapq.heappop(heap)\n        result.append(heap[0])\n    return result`,
    tests: [
      { call: "kth_largest_stream(3, [4, 5, 8, 2], [3, 5, 10, 9, 4])", expected: "[4, 5, 5, 8, 8]" },
      { call: "kth_largest_stream(1, [], [1, 2, 3])", expected: "[1, 2, 3]" },
      { call: "kth_largest_stream(2, [0], [-1, 1, 2])", expected: "[-1, 0, 1]" },
      { call: "kth_largest_stream(2, [5, 6], [7])", expected: "[6]" },
      { call: "kth_largest_stream(4, [4, 5, 8, 2], [3, 5])", expected: "[3, 4]" },
    ],
    hint: "Maintain a min-heap of size k; after each add the root is the kth largest.",
  },
  {
    id: "meeting-rooms-ii",
    title: "Meeting Rooms II",
    difficulty: "Medium",
    topic: "Heap / Priority Queue",
    statement: `Given an array of meeting time intervals [start, end), return the minimum number of conference rooms required so that no two overlapping meetings share a room.\n\nExample:\nInput: intervals = [[0, 30], [5, 10], [15, 20]]\nOutput: 2\nExplanation: [0,30] overlaps with both [5,10] and [15,20], so 2 rooms are needed.`,
    funcName: "min_meeting_rooms",
    starter: `def min_meeting_rooms(intervals):\n    # Your code here\n    pass`,
    solution: `import heapq\n\ndef min_meeting_rooms(intervals):\n    if not intervals:\n        return 0\n    intervals = sorted(intervals)\n    heap = []\n    for start, end in intervals:\n        if heap and heap[0] <= start:\n            heapq.heapreplace(heap, end)\n        else:\n            heapq.heappush(heap, end)\n    return len(heap)`,
    tests: [
      { call: "min_meeting_rooms([[0, 30], [5, 10], [15, 20]])", expected: "2" },
      { call: "min_meeting_rooms([[7, 10], [2, 4]])", expected: "1" },
      { call: "min_meeting_rooms([])", expected: "0" },
      { call: "min_meeting_rooms([[1, 5], [1, 5], [1, 5]])", expected: "3" },
      { call: "min_meeting_rooms([[1, 2], [2, 3], [3, 4]])", expected: "1" },
    ],
    hint: "Sort by start; keep a min-heap of end times, reusing a room when its meeting has ended.",
  },
  {
    id: "k-smallest-pairs",
    title: "Find K Pairs with Smallest Sums",
    difficulty: "Medium",
    topic: "Heap / Priority Queue",
    statement: `Given two ascending integer arrays nums1 and nums2 and an integer k, find the k pairs [a, b] (a from nums1, b from nums2) with the smallest sums. Return them sorted ascending (by sum, then by a, then by b).\n\nExample:\nInput: nums1 = [1, 7, 11], nums2 = [2, 4, 6], k = 3\nOutput: [[1, 2], [1, 4], [1, 6]]\nExplanation: These three pairs have the smallest sums (3, 5, 7).`,
    funcName: "k_smallest_pairs",
    starter: `def k_smallest_pairs(nums1, nums2, k):\n    # Your code here\n    pass`,
    solution: `import heapq\n\ndef k_smallest_pairs(nums1, nums2, k):\n    if not nums1 or not nums2 or k <= 0:\n        return []\n    heap = []\n    for i in range(min(k, len(nums1))):\n        heapq.heappush(heap, (nums1[i] + nums2[0], i, 0))\n    result = []\n    while heap and len(result) < k:\n        s, i, j = heapq.heappop(heap)\n        result.append([nums1[i], nums2[j]])\n        if j + 1 < len(nums2):\n            heapq.heappush(heap, (nums1[i] + nums2[j + 1], i, j + 1))\n    return sorted(result, key=lambda p: (p[0] + p[1], p[0], p[1]))`,
    tests: [
      { call: "k_smallest_pairs([1, 7, 11], [2, 4, 6], 3)", expected: "[[1, 2], [1, 4], [1, 6]]" },
      { call: "k_smallest_pairs([1, 1, 2], [1, 2, 3], 2)", expected: "[[1, 1], [1, 1]]" },
      { call: "k_smallest_pairs([1, 2], [3], 3)", expected: "[[1, 3], [2, 3]]" },
      { call: "k_smallest_pairs([], [1, 2], 3)", expected: "[]" },
      { call: "k_smallest_pairs([1, 2], [3], 0)", expected: "[]" },
    ],
    hint: "Seed the heap with pairs (nums1[i], nums2[0]); pop the smallest and advance j.",
  },
  {
    id: "nth-ugly-number",
    title: "Ugly Number II",
    difficulty: "Medium",
    topic: "Heap / Priority Queue",
    statement: `An ugly number is a positive integer whose only prime factors are 2, 3, and 5. Given n, return the nth ugly number (1-indexed, where 1 is the first ugly number).\n\nExample:\nInput: n = 10\nOutput: 12\nExplanation: The sequence begins 1, 2, 3, 4, 5, 6, 8, 9, 10, 12, so the 10th is 12.`,
    funcName: "nth_ugly_number",
    starter: `def nth_ugly_number(n):\n    # Your code here\n    pass`,
    solution: `import heapq\n\ndef nth_ugly_number(n):\n    heap = [1]\n    seen = {1}\n    val = 1\n    for _ in range(n):\n        val = heapq.heappop(heap)\n        for factor in (2, 3, 5):\n            nxt = val * factor\n            if nxt not in seen:\n                seen.add(nxt)\n                heapq.heappush(heap, nxt)\n    return val`,
    tests: [
      { call: "nth_ugly_number(1)", expected: "1" },
      { call: "nth_ugly_number(10)", expected: "12" },
      { call: "nth_ugly_number(7)", expected: "8" },
      { call: "nth_ugly_number(11)", expected: "15" },
      { call: "nth_ugly_number(15)", expected: "24" },
    ],
    hint: "Pop the smallest from a min-heap and push its multiples by 2, 3, 5, deduping with a set.",
  },
  {
    id: "connect-sticks",
    title: "Minimum Cost to Connect Sticks",
    difficulty: "Medium",
    topic: "Heap / Priority Queue",
    statement: `You have sticks with positive integer lengths. Connecting two sticks of lengths x and y costs x + y and produces a stick of length x + y. Connect all sticks into one and return the minimum total cost.\n\nExample:\nInput: sticks = [2, 4, 3]\nOutput: 14\nExplanation: Connect 2 and 3 (cost 5) -> [5, 4]; connect 5 and 4 (cost 9) -> [9]. Total = 5 + 9 = 14.`,
    funcName: "connect_sticks",
    starter: `def connect_sticks(sticks):\n    # Your code here\n    pass`,
    solution: `import heapq\n\ndef connect_sticks(sticks):\n    heap = list(sticks)\n    heapq.heapify(heap)\n    total = 0\n    while len(heap) > 1:\n        a = heapq.heappop(heap)\n        b = heapq.heappop(heap)\n        cost = a + b\n        total += cost\n        heapq.heappush(heap, cost)\n    return total`,
    tests: [
      { call: "connect_sticks([2, 4, 3])", expected: "14" },
      { call: "connect_sticks([1, 8, 3, 5])", expected: "30" },
      { call: "connect_sticks([5])", expected: "0" },
      { call: "connect_sticks([])", expected: "0" },
      { call: "connect_sticks([1, 1, 1, 1])", expected: "8" },
    ],
    hint: "Greedily connect the two shortest sticks each time using a min-heap.",
  },
  {
    id: "single-threaded-cpu",
    title: "Single-Threaded CPU",
    difficulty: "Medium",
    topic: "Heap / Priority Queue",
    statement: `You are given tasks where tasks[i] = [enqueueTime, processingTime]. A single CPU picks, among all tasks already enqueued, the one with the shortest processing time (ties broken by smaller original index). When the CPU is idle and no task is available, it jumps to the next enqueue time. Return the order in which tasks are processed, as a list of their original indices.\n\nExample:\nInput: tasks = [[1, 2], [2, 4], [3, 2], [4, 1]]\nOutput: [0, 2, 3, 1]\nExplanation: At time 1 task 0 starts (ends at 3). At time 3 tasks 1,2,3 are available; shortest is task 2 (ends at 5), then task 3 (ends at 6), then task 1.`,
    funcName: "get_order",
    starter: `def get_order(tasks):\n    # Your code here\n    pass`,
    solution: `import heapq\n\ndef get_order(tasks):\n    indexed = sorted(range(len(tasks)), key=lambda i: tasks[i][0])\n    heap = []\n    result = []\n    time = 0\n    ptr = 0\n    n = len(tasks)\n    while len(result) < n:\n        while ptr < n and tasks[indexed[ptr]][0] <= time:\n            i = indexed[ptr]\n            heapq.heappush(heap, (tasks[i][1], i))\n            ptr += 1\n        if not heap:\n            time = tasks[indexed[ptr]][0]\n            continue\n        proc, i = heapq.heappop(heap)\n        time += proc\n        result.append(i)\n    return result`,
    tests: [
      { call: "get_order([[1, 2], [2, 4], [3, 2], [4, 1]])", expected: "[0, 2, 3, 1]" },
      { call: "get_order([[7, 10], [7, 12], [7, 5], [7, 4], [7, 2]])", expected: "[4, 3, 2, 0, 1]" },
      { call: "get_order([[1, 2]])", expected: "[0]" },
      { call: "get_order([[5, 3], [1, 9], [2, 6]])", expected: "[1, 0, 2]" },
      { call: "get_order([[10, 1], [1, 1]])", expected: "[1, 0]" },
    ],
    hint: "Sort by enqueue time, advance the clock, and pop the available task with the smallest (processing time, index).",
  },
  {
    id: "smallest-range-k-lists",
    title: "Smallest Range Covering Elements from K Lists",
    difficulty: "Hard",
    topic: "Heap / Priority Queue",
    statement: `You have k sorted (ascending) integer lists. Find the smallest range [lo, hi] that includes at least one number from each list. A range [a, b] is smaller than [c, d] if b - a < d - c, or (b - a == d - c and a < c).\n\nExample:\nInput: lists = [[4, 10, 15, 24, 26], [0, 9, 12, 20], [5, 18, 22, 30]]\nOutput: [20, 24]\nExplanation: 24 from list 0, 20 from list 1, 22 from list 2 are all within [20, 24], which is the smallest such range.`,
    funcName: "smallest_range",
    starter: `def smallest_range(lists):\n    # Your code here\n    pass`,
    solution: `import heapq\n\ndef smallest_range(lists):\n    heap = []\n    cur_max = float('-inf')\n    for i, lst in enumerate(lists):\n        heapq.heappush(heap, (lst[0], i, 0))\n        cur_max = max(cur_max, lst[0])\n    best_lo, best_hi = heap[0][0], cur_max\n    while True:\n        val, i, j = heapq.heappop(heap)\n        if cur_max - val < best_hi - best_lo or (cur_max - val == best_hi - best_lo and val < best_lo):\n            best_lo, best_hi = val, cur_max\n        if j + 1 == len(lists[i]):\n            break\n        nxt = lists[i][j + 1]\n        cur_max = max(cur_max, nxt)\n        heapq.heappush(heap, (nxt, i, j + 1))\n    return [best_lo, best_hi]`,
    tests: [
      { call: "smallest_range([[4, 10, 15, 24, 26], [0, 9, 12, 20], [5, 18, 22, 30]])", expected: "[20, 24]" },
      { call: "smallest_range([[1, 2, 3], [1, 2, 3], [1, 2, 3]])", expected: "[1, 1]" },
      { call: "smallest_range([[1], [2], [3]])", expected: "[1, 3]" },
      { call: "smallest_range([[10, 10], [11, 11]])", expected: "[10, 11]" },
      { call: "smallest_range([[1, 5, 8], [4, 12], [7, 8, 10]])", expected: "[4, 7]" },
    ],
    hint: "Min-heap one element per list; track the current max, advance the list owning the min, and stop when a list is exhausted.",
  },
];
