// ---------------------------------------------------------------------------
//  patternCards.js  ·  seed flashcards for coding patterns + system-design
//  concepts. These ship as a starting deck so a fresh clone has something to
//  review immediately. They're merged into the user's deck on load (by id), so
//  adding new ones here propagates to existing users without clobbering their
//  review schedule. Content is distilled from the FrameworkGuide reference.
// ---------------------------------------------------------------------------

// type "pattern" = coding pattern, "concept" = system-design concept.
// id prefix keeps these distinct from auto-generated (p<id>) and manual (m<ts>) cards.
export const PATTERN_CARDS = [
  // ---- Coding patterns ----
  { id: "pat-sliding-window", type: "pattern", topic: "Arrays / Strings",
    front: "When do you reach for a sliding window, and what's the cost?",
    back: "Contiguous subarray/substring problems asking for a max/min/longest under a constraint. Expand the right edge, shrink the left when the constraint breaks. O(n) time, O(1)–O(k) space. e.g. Longest Substring Without Repeating." },
  { id: "pat-two-pointers", type: "pattern", topic: "Arrays",
    front: "Two-pointer pattern: when and how?",
    back: "Sorted array / palindrome / pair-sum problems. Move pointers inward (or fast/slow). Turns an O(n²) scan into O(n). e.g. Two Sum II, 3Sum, Valid Palindrome." },
  { id: "pat-fast-slow", type: "pattern", topic: "Linked List",
    front: "Fast & slow pointers — what does it detect?",
    back: "Cycle detection (Floyd's), finding the middle node, and cycle entry point. Slow moves 1, fast moves 2; they meet inside a cycle. O(n) time, O(1) space." },
  { id: "pat-bfs-vs-dfs", type: "pattern", topic: "Graphs / Trees",
    front: "BFS vs DFS — how do you pick?",
    back: "BFS (queue) for shortest path in unweighted graphs and level-order. DFS (stack/recursion) for connectivity, cycle detection, topological sort, backtracking. Both O(V+E)." },
  { id: "pat-backtracking", type: "pattern", topic: "Recursion",
    front: "Backtracking template — what are the moving parts?",
    back: "Choose → explore (recurse) → un-choose. Used for subsets, permutations, combinations, N-Queens. Prune invalid branches early. Often O(branching^depth)." },
  { id: "pat-binary-search", type: "pattern", topic: "Search",
    front: "Beyond sorted arrays, when is binary search the answer?",
    back: "Whenever the answer space is monotonic — 'binary search on the answer'. e.g. min capacity, rotated arrays, first/last position. O(log n) per search." },
  { id: "pat-heap-topk", type: "pattern", topic: "Heaps",
    front: "Top-K / streaming median — which structure?",
    back: "A heap of size k: min-heap for top-K largest, max-heap for top-K smallest. Two heaps (max+min) for a running median. Push/pop O(log k)." },
  { id: "pat-dp-recognition", type: "pattern", topic: "Dynamic Programming",
    front: "How do you recognize a DP problem and start it?",
    back: "Overlapping subproblems + optimal substructure; counting paths / min cost / can-you-reach. Define the state, write the recurrence, decide memo (top-down) vs tabulation (bottom-up). e.g. Coin Change, LIS." },
  { id: "pat-union-find", type: "pattern", topic: "Graphs",
    front: "Union-Find (DSU): use case and complexity?",
    back: "Dynamic connectivity, counting connected components, cycle detection in undirected graphs, Kruskal's MST. With path compression + union by rank: near-O(1) amortized (α(n))." },
  { id: "pat-monotonic-stack", type: "pattern", topic: "Stacks",
    front: "Monotonic stack — what problems does it crack?",
    back: "Next-greater / next-smaller element, daily temperatures, largest rectangle in histogram. Keep the stack increasing or decreasing; pop while the invariant breaks. O(n)." },
  { id: "pat-intervals", type: "pattern", topic: "Intervals",
    front: "Interval problems — the standard first move?",
    back: "Sort by start (or end). Then sweep: merge when overlapping, count rooms with a min-heap of end times, or greedily pick by earliest end. e.g. Merge Intervals, Meeting Rooms II." },
  { id: "pat-trie", type: "pattern", topic: "Tries",
    front: "When is a trie worth the overhead?",
    back: "Prefix queries, autocomplete, word-search/dictionary problems, and bitwise-XOR maximization. Insert/search O(L) in the key length, independent of how many keys are stored." },

  // ---- System-design concepts ----
  { id: "con-cap", type: "concept", topic: "Distributed Systems",
    front: "State CAP correctly (not 'pick 2').",
    back: "During a network Partition you must choose between Consistency (reject/stall to stay correct) and Availability (serve possibly-stale data). With no partition you can have both. It's about behavior under partition, not a permanent menu." },
  { id: "con-consistency", type: "concept", topic: "Distributed Systems",
    front: "Strong vs eventual consistency — trade-off?",
    back: "Strong: every read sees the latest write (needs coordination/quorums → higher latency, lower availability). Eventual: replicas converge over time (low latency, high availability, reads may be stale). Pick per data criticality." },
  { id: "con-cache-strategies", type: "concept", topic: "Caching",
    front: "Cache-aside vs write-through vs write-back?",
    back: "Cache-aside: app loads on miss, writes invalidate (simple, stale risk). Write-through: write cache+DB together (consistent, slower writes). Write-back: write cache, flush later (fast, data-loss risk on crash)." },
  { id: "con-consistent-hashing", type: "concept", topic: "Scaling",
    front: "Why consistent hashing for caches/shards?",
    back: "Plain hash(key) % N remaps almost everything when N changes. Consistent hashing maps nodes+keys onto a ring so adding/removing a node only moves ~1/N of keys. Virtual nodes smooth out hot spots." },
  { id: "con-load-balancing", type: "concept", topic: "Scaling",
    front: "L4 vs L7 load balancing?",
    back: "L4 balances by IP/port (fast, protocol-agnostic, no payload inspection). L7 understands HTTP — can route by path/header/cookie, do TLS termination and sticky sessions, at higher cost." },
  { id: "con-db-scaling", type: "concept", topic: "Databases",
    front: "Replication vs sharding — what does each buy you?",
    back: "Replication (leader-follower) scales READS and adds redundancy; followers lag. Sharding (range/hash/geo) scales WRITES and storage by splitting data across nodes; introduces cross-shard query/resharding pain." },
  { id: "con-sql-nosql", type: "concept", topic: "Databases",
    front: "How do you justify SQL vs NoSQL in an interview?",
    back: "Tie it to the access pattern, not hype. SQL: relations, transactions, ad-hoc queries, strong consistency. NoSQL: KV/document/wide-column/graph for known access paths, massive scale, flexible schema, tunable consistency." },
  { id: "con-queues", type: "concept", topic: "Messaging",
    front: "Message queue: at-least-once vs exactly-once?",
    back: "At-least-once is the practical default — consumers must be idempotent because retries cause duplicates. 'Exactly-once' is really at-least-once + dedup/idempotency keys. Kafka = ordered log/replay; SQS = simple work queue." },
  { id: "con-idempotency", type: "concept", topic: "Reliability",
    front: "Idempotency keys — what problem do they solve?",
    back: "Network retries can replay a request (double charge, double order). The client sends a unique key; the server records it and returns the same result for repeats. Essential for payments and any non-GET retry." },
  { id: "con-estimation", type: "concept", topic: "Framework",
    front: "Back-of-envelope: what numbers should be instant?",
    back: "QPS = DAU × actions/day ÷ 86,400 (×2–3 peak). Storage = items × size × retention × replication. 1 server ≈ a few k QPS. Memory/SSD/network latencies differ by orders of magnitude — quote them unprompted." },
  { id: "con-7step", type: "concept", topic: "Framework",
    front: "Recite the 7-step system-design framework.",
    back: "1) Requirements (functional + non-functional + scale) → 2) Estimates → 3) API → 4) Data model → 5) High-level design → 6) Deep dive on 1–2 components → 7) Trade-offs / bottlenecks / failure modes." },
  { id: "con-cdn", type: "concept", topic: "Scaling",
    front: "What does a CDN actually do, and its hard part?",
    back: "Caches static (and some dynamic) content at edge PoPs near users → lower latency, less origin load. The hard part is invalidation: TTLs, versioned URLs, or purge APIs to avoid serving stale assets." },
];
